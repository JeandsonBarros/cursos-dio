{
  "devDependencies": {
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.3.2"
  },
  "scripts": {
    "start": "npx tsc && node build/index.js",
    "dev" : "ts-node-dev --respawn --transpile-only src/index.ts"
  }
}
